<Page
    x:Class="LadyLendar.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LadyLendar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <DatePicker HorizontalAlignment="Left" Margin="50,50,0,0" VerticalAlignment="Top" DateChanged="DatePicker_DateChanged"/>
        <Button x:Name="periodStartsBtn" Content="Period starts" HorizontalAlignment="Left" Margin="250,100,0,0" VerticalAlignment="Top" Click="periodStartsBtn_ClickAsync"/>

        <TextBlock x:Name="allPeriodsDataTitle" HorizontalAlignment="Left" Margin="50,250,0,0" Text="All Periods Data" TextWrapping="Wrap" VerticalAlignment="Top" Width="256" Height="32"/>
        <ItemsControl Name="lstMachineFunctions" HorizontalAlignment="Left" Height="428" Margin="50,282,0,0" VerticalAlignment="Top" Width="290">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBox Text="{Binding dateType}"/>
                        <TextBox Text="{Binding dateValueToString}"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
